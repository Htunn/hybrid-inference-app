# URL of the backend proxy.
# Development (local): point to the local FastAPI server.
# Docker production:   leave empty — Nginx proxies /api/ to the backend container.
VITE_API_URL=http://localhost:8000
