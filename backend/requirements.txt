fastapi>=0.111.0
uvicorn[standard]>=0.30.0
gunicorn>=23.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9  # Required for file uploads

# Inference backends
google-generativeai>=0.8.0  # Gemini
openai>=1.50.0  # OpenAI
# vllm>=0.7.0  # Uncomment if running vLLM in-process (heavy dependency)

# Monitoring & metrics
prometheus-client>=0.21.0

# Configuration
toml>=0.10.2

# RAG pipeline
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pgvector>=0.3.0

# PyTorch CPU-only (for Mac M3 / systems without NVIDIA GPU)
# This avoids downloading 444MB of CUDA libraries
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

sentence-transformers>=3.0.0
pypdf>=4.0.0
